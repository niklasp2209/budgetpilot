name: Build Images

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master

jobs:
  build-docker-images:
    runs-on: ubuntu-latest
    if: ${{ false }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Placeholder
        run: echo "Docker image build is scaffolded and can be enabled later."
