spring:
  application:
    name: budgetpilot-backend

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true

auth:
  jwt:
    secret: change-me-change-me-change-me-change-me
    access-ttl-seconds: 900
    refresh-ttl-seconds: 1209600
